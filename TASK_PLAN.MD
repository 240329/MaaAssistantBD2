# 棕色尘埃2助手整体规划与编排

> 文档状态：规划基线
>
> 创建日期：2026-08-17
>
> 约束：本文件创建后，除用户明确授权外不得修改。后续实现以本文件为依据；需求变化应先由用户确认是否更新规划。

## 1. 项目目标

构建一个面向 Windows 桌面端《棕色尘埃2》的自动化助手。第一阶段优先完成可运行、可扩展的 UI 和任务编排框架；第二阶段再逐项接入 Win32 窗口绑定、截图识别、输入控制和具体游戏流程。

目标不是一次性实现所有功能，而是建立以下长期能力：

- 通过 Win32 绑定到指定游戏窗口，不依赖窗口必须位于前台。
- 将每个游戏功能实现为独立、可启停、可排序、可配置、可重试的任务模块。
- 用统一的运行状态、日志、截图和错误报告承载所有功能。
- 将页面导航等稳定流程与复杂业务策略分离，为后续资源化任务流程留下空间。
- 允许未来增加账号、版本、活动、识别模型、任务插件和新 UI 页面，而不重写任务调度器。

## 2. 当前功能范围

下列功能进入产品规划，但具体点击路径、识别规则、异常处理和参数默认值暂不锁定，待后续连接真实游戏窗口后逐项确认。

### 2.1 日常与领取

- 每日自动抽角色
- 自动抽武器
- 公会签到
- 收菜
- 邮箱领取
- 通行证领取
- 每日任务奖励
- 每周任务奖励
- 女神像签到

### 2.2 战斗与挑战

- 魔兽挑战
- 活动战斗
- 米饭战斗
- 火把战斗
- 狩猎场战斗
- 压制
- 偷钱
- 每周地图任务

### 2.3 商店与互动

- 活动商店自动购买
- 广场商品购买
- 酒馆亲密度

### 2.4 特殊收集与吸取

- 收集物吸取
- 葛罗提吸取

### 2.5 竞技

- 自动 PVP

## 3. 总体架构

采用“桌面 UI + 应用编排层 + 自动化核心 + Win32 Controller + 资源/策略”的分层结构。

```text
┌─────────────────────────────────────────────────────────┐
│ Desktop UI                                              │
│ 任务队列 / 功能配置 / 运行日志 / 连接管理 / 设置          │
└──────────────────────┬──────────────────────────────────┘
                       │ typed commands + typed events
┌──────────────────────▼──────────────────────────────────┐
│ Application Orchestration                                │
│ TaskScheduler / RunState / EventBus / Persistence         │
│ 任务依赖、优先级、重试、取消、计划、结果聚合               │
└──────────────────────┬──────────────────────────────────┘
                       │ task contract
┌──────────────────────▼──────────────────────────────────┐
│ Automation Core                                          │
│ Win32Controller / Screenshot / Input / Vision / Pipeline │
│ DomainTask plugins / ResourceLoader / Recovery            │
└──────────────────────┬──────────────────────────────────┘
                       │ HWND + normalized coordinates
┌──────────────────────▼──────────────────────────────────┐
│ BrownDust2 Win32 Window                                  │
└─────────────────────────────────────────────────────────┘
```

### 3.1 UI 层

职责：

- 展示和编辑任务队列。
- 展示每个功能的配置表单和启用状态。
- 管理窗口绑定选择、连接状态和截图预览。
- 展示运行进度、当前步骤、日志、警告、截图和错误详情。
- 管理主题、语言、通知、开机启动和数据目录等桌面设置。

UI 不得直接调用 Win32 API、发送鼠标消息、做 OCR 或决定业务流程。所有操作通过应用服务和 typed command 完成。

### 3.2 应用编排层

核心对象建议包括：

- `TaskDefinition`：功能元数据、名称、分类、参数 schema、能力要求。
- `TaskPlan`：用户排列后的任务实例列表、启用状态、依赖和执行策略。
- `TaskScheduler`：串行/受限并行执行、取消、暂停、延迟、重试和失败策略。
- `TaskContext`：窗口句柄、截图服务、输入服务、识别服务、配置和取消令牌。
- `TaskResult`：完成、跳过、失败、取消、需要人工确认等结果。
- `AutomationEvent`：任务开始、步骤更新、日志、截图、异常、完成和连接变化。
- `RunState`：未连接、已连接、空闲、运行中、暂停中、停止中、完成、失败。

所有功能都通过统一任务契约接入，禁止功能模块自行创建线程或绕过调度器。

### 3.3 自动化核心

自动化核心分为以下能力：

- `Win32WindowService`：枚举窗口、选择 HWND、读取标题/进程/客户区、监听窗口销毁和尺寸变化。
- `Win32Controller`：绑定目标 HWND，提供截图、点击、移动、按键、文本输入和窗口状态操作。
- `CoordinateMapper`：在逻辑坐标、客户区坐标、DPI 缩放和截图坐标之间转换。
- `ScreenshotService`：统一截图格式、缓存、裁剪、缩放和调试保存。
- `VisionService`：模板匹配、OCR、像素/颜色判断、区域检测和后续模型扩展。
- `PipelineEngine`：执行可配置的识别、动作、等待、分支、重试和回退节点。
- `RecoveryService`：统一处理窗口丢失、截图失败、页面不匹配、超时和不可恢复错误。

### 3.4 功能模块

每个功能模块独立实现 `ITaskModule` 或等价任务契约，并按域分组：

```text
Features/
├─ Daily/       抽角色、抽武器、签到、收菜、邮箱、通行证、任务奖励
├─ Battle/      魔兽、活动、米饭、火把、狩猎场、压制、偷钱、每周地图
├─ Shop/        活动商店、广场商品
├─ Collection/  收集物吸取、葛罗提吸取
├─ Social/      工会、酒馆亲密度、女神像
└─ Pvp/         自动 PVP
```

功能模块只描述业务意图和流程，不直接依赖具体 HWND 查找实现或 UI 控件。

## 4. Win32 窗口绑定规划

Win32 窗口绑定是明确的技术约束，第一版不采用 ADB 或模拟器控制作为主路径。

### 4.1 绑定流程

1. 枚举可见顶层窗口。
2. 显示窗口标题、进程名、进程 ID、类名和客户区尺寸。
3. 用户选择目标窗口。
4. 校验窗口仍然存在且可截图。
5. 建立 `WindowBinding`，保存 HWND、进程信息、初始客户区矩形和 DPI 信息。
6. 定期检测 HWND 有效性、窗口尺寸、最小化状态和渲染变化。
7. 游戏窗口销毁或变化超出支持范围时，发布连接事件并停止当前任务。

### 4.2 截图策略

按能力探测顺序设计适配层，具体实现阶段验证：

- Windows Graphics Capture 或等价窗口捕获路径。
- PrintWindow 作为兼容回退。
- BitBlt/桌面 DC 仅作为最后回退，不假设它能捕获硬件加速游戏画面。

截图服务必须：

- 明确截图是否包含客户区、边框和黑边。
- 输出统一像素格式。
- 提供原图、逻辑分辨率图和 ROI 图。
- 记录截图耗时和失败原因。
- 在调试模式保存脱敏后的失败截图和识别标记。

### 4.3 输入策略

所有输入统一走 `IInputController`，不在业务代码中直接调用 `PostMessage` 或 `SendInput`。

输入能力分级：

- 窗口消息点击/键盘：优先验证对游戏是否有效。
- `SendInput`：仅在明确需要前台输入时启用，并在 UI 中显示该限制。
- 其他 Win32 输入方式：以能力探测和可配置策略接入，不在第一阶段预埋无验证实现。

必须记录输入的逻辑坐标、映射后的客户区坐标、动作类型和结果，方便复现误操作。

### 4.4 坐标与 DPI

内部统一使用逻辑基准坐标，例如 `1920x1080` 或实际确认的游戏画布尺寸；禁止功能模块写死当前窗口像素坐标。

`CoordinateMapper` 负责：

- 客户区与截图尺寸映射。
- DPI 缩放和 Windows 缩放设置。
- 游戏内部黑边或非 16:9 画布适配。
- ROI、点击位置和滑动路径的转换。
- 窗口尺寸变化后的重新校准。

第一阶段 UI 需要提供截图预览和坐标/尺寸诊断信息，方便确定棕色尘埃2实际渲染方式。

## 5. 任务编排模型

### 5.1 统一任务生命周期

```text
Created
  → Ready
  → WaitingForBinding
  → Running
  → Paused
  → Stopping
  → Completed / Skipped / Failed / Cancelled / NeedsUser
```

每个任务步骤应具备：

- 稳定 ID 和显示名称。
- 前置条件和页面/状态假设。
- 识别策略和动作。
- 超时、最大重试次数和回退步骤。
- 是否允许跳过。
- 调试截图和结构化日志。

### 5.2 调度策略

默认采用单窗口串行执行，避免多个功能同时操作同一个游戏窗口。

调度器需要预留：

- 任务优先级。
- 任务依赖，例如先进入主界面再领取邮箱。
- 运行前置检查，例如窗口绑定、分辨率和登录状态。
- 失败后继续、停止任务链或等待人工确认三种策略。
- 每日/每周计划和任务去重。
- 任务冷却时间，防止重复领取或重复购买。
- 手动停止和安全退出。

### 5.3 任务组编排

第一版建议使用固定任务组与可排序队列：

```text
启动检查
→ 主界面确认
→ 日常领取组
→ 商店组
→ 战斗组
→ 特殊收集组
→ PVP 组
→ 每周任务组
→ 收尾与结果汇总
```

以上只是默认模板。用户必须能够启用、禁用、排序和单独运行任务；后续再提供自定义任务组、条件分支和计划任务。

### 5.4 数据驱动扩展

稳定的页面导航建议逐步资源化，例如：

```json
{
  "id": "daily.mail.claim",
  "preconditions": ["main.home"],
  "steps": [
    {
      "id": "open_mail",
      "detect": { "type": "template", "name": "mail_button" },
      "action": { "type": "click_self" },
      "next": ["claim_all"]
    }
  ],
  "retry": 2,
  "on_error": "return_home"
}
```

示例只定义方向，不代表当前已锁定的协议。具体 schema 应在 UI 和 Core 第一轮纵切验证后再确定。

复杂逻辑，例如活动商店购买优先级、PVP 阵容选择、战斗策略和资源判断，应由代码模块或策略服务处理，避免 DSL 变成不可维护的编程语言。

## 6. UI 优先阶段规划

### 阶段 0：产品与技术基线

目标：在写 UI 前锁定最小可行技术路线。

产出：

- Windows 桌面 UI 框架选型。
- 解决方案和目录结构。
- 配置、日志、数据目录和资源目录约定。
- 任务契约、事件契约和运行状态初稿。
- Win32 绑定能力验证清单。

约束：不能因为参考 MAA 就默认复制 WPF；应根据本项目的实际运行环境、维护成本和 Win32 集成需求选择技术。

### 阶段 1：UI 骨架与视觉系统

目标：先完成可启动、可操作、无真实自动化的 UI。

页面：

- 主工作台：任务队列、当前任务配置、运行日志。
- 窗口绑定页：窗口枚举、选择、绑定状态、尺寸和截图预览占位。
- 功能目录页：按日常、战斗、商店、收集、社交、PVP 分类展示功能。
- 设置页：主题、语言、日志级别、调试目录和安全选项。

交互：

- 创建、启用、禁用、排序、复制和删除任务项。
- 每个功能先展示参数占位和“逻辑待实现”状态。
- 模拟运行模式产生虚拟进度、日志、成功、跳过和失败事件。
- 统一显示开始、暂停、停止和运行状态。
- 所有关键操作有键盘和可见按钮入口，不依赖 hover 或右键。

验收：

- UI 可启动，窗口尺寸变化不产生控件重叠。
- 任务队列操作和配置持久化可用。
- 模拟任务可完整跑通事件流。
- 不引入真实游戏点击，不声称任何游戏功能已实现。

### 阶段 2：Win32 窗口绑定纵切

目标：完成“枚举窗口 → 绑定 HWND → 截图预览 → 解除绑定”的最小闭环。

产出：

- `Win32WindowService`。
- `WindowBinding` 状态模型。
- 截图能力探测和统一图像格式。
- 客户区尺寸、DPI、窗口状态诊断。
- 绑定丢失和窗口变化事件。

验收：

- 可选择棕色尘埃2窗口。
- 窗口移动、缩放、最小化、关闭时 UI 状态正确更新。
- 在支持路径下获得稳定截图。
- 失败时展示明确原因，不自动进入不确定的输入操作。

### 阶段 3：输入与视觉基础设施

目标：完成可测试的截图、坐标和输入抽象，但先只用诊断工具验证。

产出：

- `CoordinateMapper`。
- `IInputController` 和 Win32 实现。
- 模板匹配、OCR、像素判断接口。
- ROI 标注、截图保存和识别结果叠加预览。
- fake Controller 和 fixture 测试。

验收：

- 在测试图片上验证 ROI、模板和 OCR 结果。
- 坐标映射在至少两种窗口尺寸/DPI 下稳定。
- 诊断点击模式必须由用户明确触发，并显示点击位置。

### 阶段 4：第一个真实功能纵切

优先选择风险低、闭环短、容易人工确认的功能，建议顺序：

1. 女神像签到或公会签到。
2. 邮箱领取。
3. 每日/每周任务奖励。
4. 通行证领取。

每个功能必须完整包含：UI 配置、任务注册、页面识别、Win32 操作、成功判断、重试、失败截图、日志和人工停止。

阶段验收不是“点击成功”，而是能解释任务为什么开始、当前在哪一步、为什么完成或失败。

### 阶段 5：日常领取与商店

实现：

- 收菜。
- 自动抽角色。
- 自动抽武器。
- 活动商店自动购买。
- 广场商品购买。
- 酒馆亲密度。
- 收集物吸取和葛罗提吸取。

先实现页面导航和可配置选项，再实现购买优先级、资源阈值和重复执行保护。涉及抽取和购买的功能必须提供预览、确认和限额，默认避免不可逆操作。

### 阶段 6：战斗与挑战

实现：

- 魔兽挑战。
- 活动战斗。
- 米饭战斗。
- 火把战斗。
- 狩猎场战斗。
- 压制。
- 偷钱。
- 每周地图任务。

采用“战斗入口状态图 + 战斗策略模块 + 结果识别”的结构。每种战斗先支持单次、手动停止和失败截图，再增加次数、资源消耗、自动重试和批量计划。

### 阶段 7：自动 PVP 与复杂策略

自动 PVP 作为高风险复杂模块单独推进：

- 页面状态识别和战斗结果判断。
- 对手/阵容信息读取。
- 阵容或策略配置。
- 失败和异常匹配处理。
- 次数、冷却和赛季状态。

在策略逻辑明确前，只做 UI 配置和模拟运行，不实现不可逆的自动点击。

### 阶段 8：计划任务、更新和扩展生态

最后完善：

- 每日/每周计划。
- 任务去重和运行历史。
- 资源和流程版本管理。
- 配置导入导出。
- 自定义任务流程。
- 插件/功能模块注册机制。
- 诊断包和可控日志上传。

## 7. UI 信息架构草案

### 主工作台

```text
┌──────────────────────────────────────────────────────────┐
│ 顶栏：程序状态 | 目标窗口 | 绑定/解除 | 设置              │
├───────────────┬──────────────────┬───────────────────────┤
│ 任务队列      │ 功能配置          │ 运行控制台             │
│ 分类/排序      │ 当前任务          │ 当前步骤               │
│ 启用/禁用      │ 常规/高级         │ 日志/警告/截图         │
│ 批量操作       │ 逻辑待实现提示    │ 进度/耗时/结果         │
├───────────────┴──────────────────┴───────────────────────┤
│ 开始 | 暂停 | 停止 | 模拟运行 | 打开日志目录              │
└──────────────────────────────────────────────────────────┘
```

### 功能配置

每个功能统一包含：

- 启用开关和任务说明。
- 运行条件。
- 运行次数或时间范围。
- 资源上限和购买/抽取策略。
- 失败处理。
- 高级识别和调试选项。
- 当前实现状态：未实现、模拟可用、绑定验证中、正式可用、需要人工确认。

### 连接与诊断

- 窗口列表。
- 绑定状态和 HWND 信息。
- 游戏客户区尺寸、DPI 和坐标基准。
- 实时截图。
- 截图方式和输入方式能力。
- 测试截图、测试点击、停止绑定。

## 8. 风险与未决问题

以下问题必须在实现对应阶段确认，不在本次规划中擅自假设：

- 游戏是独立 Win32 窗口、模拟器窗口还是其他宿主窗口。
- 窗口是否允许后台截图和后台输入。
- 是否必须使用前台输入；若是，运行期间用户能否操作鼠标键盘。
- 游戏分辨率、DPI、缩放、黑边和窗口模式。
- OCR 对中文、数字、角色名和道具名的稳定性。
- 抽取、购买、PVP 等不可逆操作的确认和限额规则。
- 活动内容变化频率以及资源/流程更新方式。
- 是否支持多账号、多个游戏窗口或仅支持单窗口。
- 是否需要开机启动、托盘、远程控制和外部通知。
- 目标应用技术栈、运行时版本和打包方式。

安全边界：

- 不保存或收集账号密码、Token、Cookie 等敏感凭据。
- 不绕过游戏客户端的安全校验或修改游戏文件。
- 不默认执行抽取、购买、PVP 等高风险动作。
- 所有自动化操作必须可停止，失败时优先停机并保留诊断信息。
- 资源和流程只在用户明确提供或合法可用的范围内维护。

## 9. 规划完成标准

本文件完成后，后续实现应以以下顺序推进：

1. 先完成阶段 0 的技术选型和最小目录设计。
2. 再完成阶段 1 的 UI 骨架与模拟任务闭环。
3. 用户确认 UI 方向后，进入阶段 2 的 Win32 绑定纵切。
4. 用户确认窗口绑定和截图稳定后，进入阶段 3 的视觉/输入基础设施。
5. 每次只推进一个或一组可验证功能，不把所有功能一次性写成未验证的点击脚本。

本计划不定义具体游戏坐标、模板、OCR 文本、战斗策略或购买优先级；这些内容属于后续实现阶段的事实和决策，应记录在对应代码、资源和测试中。
